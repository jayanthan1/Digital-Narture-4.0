{
  "name": "bootstrap-sass-demo",
  "version": "1.0.0",
  "description": "Bootstrap 5 with Sass customization demo",
  "main": "index.js",
  "scripts": {
    "sass": "sass scss/custom.scss css/styles.css --watch",
    "build": "sass scss/custom.scss css/styles.css --style compressed"
  },
  "dependencies": {
    "bootstrap": "^5.3.0"
  },
  "devDependencies": {
    "sass": "^1.62.0"
  }
}
